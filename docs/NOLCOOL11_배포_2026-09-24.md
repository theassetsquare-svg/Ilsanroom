# 놀쿨11 배포 기록 — 0일째(배포 준비) · 2026-09-24

> 11-6 지시서 1절의 「각 날 기록」 첫 장. **이 날은 배포하지 않았다.** 배포 버튼(= main 으로 올리기)은 대표님 한 줄로 남겼다(아래 3절).

## 1. 배포할 판
| 항목 | 값 |
|---|---|
| 저장소 · 가지 | `theassetsquare-svg/Ilsanroom` · worktree 가지 `nol11-2` |
| 기준(지금 라이브) | origin/main `38be1ef` — 09-24 fetch 로 확인, 그 뒤 새 커밋 0 → 빨리감기(fast-forward)로 올라간다 |
| 올릴 커밋 | 11-2 `fa0b2c8` · 11-3 `4aeb895` · 11-4 `500d851` · 11-5 `f4ea15b` · 배포 준비(이 문서와 같은 커밋) |
| 로컬 빌드 | 36회차 exit 0 · 게이트 41개 + page-gate 466쪽 막음 0 |
| 사이트맵 | 466 = 466(빠짐 0 · 추가 0) · 라우트 수 102 그대로 |
| 바뀌는 쪽 | **466/466**(커밋된 콘텐츠 해시 기준) — 시리즈가 모든 쪽의 제목·뼈대·JSON-LD·다음에 볼 곳을 바꿨다 |
| 배포 길 | Cloudflare Pages 자동 빌드(main 에 올라가면 Pages 가 `npm run build`) — Actions 사용 분 0 |

## 2. 배포 전 로컬 실측(한 번 돌고 끝나는 측정 · Lighthouse 모바일)
| 쪽 | 점수 | LCP(ms) | CLS | TBT(ms) | 콘솔 오류 |
|---|---|---|---|---|---|
| / | 53 | 9,790 | 0.002 | 747 | 0 |
| /nights/ilsanshampoonight/ | 63 | 8,733 | 0 | 423 | 0 |
| /region/강남/ | 75 | 4,488 | 0 | 364 | 0 |
| /community/ | 75 | 4,207 | 0 | 385 | 0 |
| /login/ | 76 | 3,922 | 0 | 445 | 0 |
| /my/favorites/ | 70 | 4,457 | 0 | 496 | 0 |

- **CLS 회귀를 고쳤다**: 11-3 에서 0.10~0.13 이던 것이 0~0.002. 원인은 짧은 정적 첫 화면 아래의 본문이 React 가 첫 화면을 갈아끼울 때 밀려 내려간 것이었다. `#root` 가 처음부터 첫 화면 높이(100svh)를 차지하게 했다. 숨긴 것 0 · 내용 변화 0.
- **LCP 2.5초는 아직 못 넘는다**(홈 9.8초 · 가게 8.7초). 라이브도 같은 수준(11-3 전 측정 홈 9.9초)이라 회귀는 아니다. 원인은 React 가 첫 그림을 다시 그리는 구조다 → 배포 뒤 과제로 남긴다(지시서 P5 「100%」는 이 때문에 통과하지 못한다).
- 로그인·찜 목록(11-5 새 화면) 쪽을 포함한 6쪽 모두 콘솔 오류 0.

## 3. 대표님 한 줄 — 배포(= main 으로 올리기)
플랫폼 규칙(P8 · 「놀쿨 배포는 클로드가 하지 않는다」)이라 이 한 줄은 대표님이 치신다.
```
! git -C D:/naver-watch/wt-nolcool push origin nol11-2:main
```
- 올린 뒤 Cloudflare Pages 가 빌드한다. 끝나면 제가 라이브 확인(유형별 표본 · PSI · 서치콘솔 URL 검사 5 · IndexNow Bing)을 이어서 한다.
- **되돌리기**: Cloudflare 대시보드 → Workers & Pages → 놀쿨 프로젝트 → Deployments → `38be1ef` 배포 → 「Rollback to this deployment」. 저장소는 그대로 두고 화면만 옛 판으로 돌아간다.

## 4. 지시서와 다르게 해야 하는 것(제안 · 대표님 결정)
- **「하루 한 유형」으로 나눠 올릴 수 없다.** 결과물이 한 빌드에서 466쪽 전부 나온다. 유형별로 나누려면 유형마다 옛 코드(숨김 본문 · 클로킹 위험)를 되살리는 분기가 필요하다.
- 제안: **올리기는 1번(전부)**, 라이브 확인을 하루 한 유형씩 6일(홈 → 허브·목록 → 가게 절반 → 가게 나머지 → 매거진·가이드 → 커뮤니티·회원). 문제가 나오면 그날 되돌리기(롤백 1클릭).
- 이 제안은 「하루 배포 1회」는 지키고 「사람 페이스」는 확인·조치 속도로 지킨다.

## 5. 올린 뒤 바깥으로 나가는 것
- IndexNow: 창구 **Bing 하나** · **바뀐 쪽만**(lastmod 가 그 빌드 날짜인 쪽). 세 창구에 466쪽 전부를 매 빌드 보내던 것을 고쳤다. 첫 배포 날에는 `scripts/.seo-lastmod.json` 을 커밋해 두어 Pages 빌드는 0건을 보내고, 확인이 끝난 뒤 `scripts/indexnow.mjs`(변경분 · Bing)로 한 번 보낸다.
- 네이버: 0(수집요청·IndexNow 창구 모두 없음).

## 6. 보안 — 대표님 확인
- worktree 의 git 원격 주소에 **GitHub 개인 토큰이 그대로 박혀** 있다(`git remote -v` 에 보임). 토큰 교체(GitHub → Settings → Developer settings → Personal access tokens → 재발급)와, 원격 주소를 토큰 없는 주소로 바꾸는 것을 권한다.
- 11-5 에서 적은 두 가지: `/api/notification` 에 인증·횟수 제한 없음 · 네이버 인증 함수 state 미확인.


---

# 1일째 — 2026-09-24 (배포 1회 · 롤백 0)

## 배포
- 11:12 대표님이 `git push origin nol11-2:main`(38be1ef..611105c) → Cloudflare Pages 빌드 → **11:14 라이브 반영**(새 판 표식 `data-nc-fold` 확인).

## 라이브 확인 (`scripts/verify/nc11-6-live.mjs` · 사람 속도 1.5초 간격)
| 항목 | 결과 |
|---|---|
| 라이브 사이트맵 466 = 배포 전 466 · 빠짐 0 · 추가 0 | 통과 |
| robots(모든 로봇 허용 · AI 봇 6종 · Sitemap) · llms.txt 전체 466(제목+직답) · IndexNow 키 파일 | 통과 |
| 유형별 표본 30쪽: 200 · 새 판 · 로컬과 같은 제목 · 쪽 게이트 막음 0 | 30/30 |
| 합계 | **134항목 · 실패 0** |

## 실제 화면 (크롬 · 라이브)
| 화면 | 결과 |
|---|---|
| 홈 맨 위 검색창 | 보임 · 「강남」 입력 → 자동완성 8개 · 제출 → `/search?q=강남` 결과 16 |
| 홈 자동완성 | **결함** — 8개 중 4개만 보이고 첫 화면 칸 경계에서 잘림 → 2일째 판에서 고침 |
| 로그인 쪽 | 필수 3 · 선택 2(꺼진 채) · 필수 없이 카카오 누르면 「필수 항목 3개에 체크해 주세요」로 막힘 · 원래 쪽 복귀 주소 저장 · 간주 문구 0 |
| 로그인 쪽 탭 제목 | **결함** — 「카카오·네이버·구글 1분이면 로그인 끝난다」(없는 네이버) → 2일째 판에서 고침 |
| 가게 쪽(일산샴푸) | 다음에 볼 곳 8칸 · 저장 단추 진짜 클릭 저장/해제 동작 · 내부 새 창 0 |
| 비교 | 가게 쪽 링크 → 세 곳(일산샴푸·대전원·강서호박) 나란히 |

## 서치콘솔 URL 검사 표본 5 (공식 도구 · 크롬)
홈 · 가게(일산샴푸) · 지역(강남) · 매거진(대전) · 커뮤니티 — **5/5 「URL이 Google에 등록되어 있음 · 페이지 색인 생성됨」**(배포 전 크롤 기준). 개요: 색인 **601** · 미색인 **1,595**(11-1 시작선 536 · 1,586).

## CWV
- PSI 공식 API: **429(일일 할당량)** — 도구 한도(모델 한도 아님). 대신 한 번 돌고 끝나는 Lighthouse(모바일) 16쪽: CLS 0~0.002 · 콘솔 오류 0 · LCP 홈 9.0초·가게 6.2~8.4초(2.5초 기준 미달 — 구조 과제).

## IndexNow (Bing)
- `scripts/indexnow.mjs`(바뀐 쪽 466 · Bing 하나) → **403 Forbidden**. 키 파일은 라이브 200·값 일치 → Bing 의 키 확인 요청이 Cloudflare 봇 방어에 막히는 것으로 본다. 다시 보내지 않았다.

## 같은 날 들어온 긴급 지시 — 수원찬스돔 연락처 (긴급 수원찬스돔 지시서 2-1)
- 라이브에 명단 밖 옛 닉네임·번호가 있었다(장부 `src/data/venues.ts:799-800` · 매거진 1곳 · 썸네일 카드 그림 속 글자). 광고주 기준(`data/advertisers.json`) = **박찬호 · 010-7117-5077**.
- 고침: 장부·매거진 · 썸네일을 표준 4줄 카드로 다시 그림(`public/og/suwonchancenight-v6.jpg` · 판 번호 -v6) · 옛 카드 생성 표·SVG·감시 토큰·시드 SQL 정리 · 소스 전체 grep 0(backups/ 옛 날짜 백업만 예외).
- 광고주 10명 대조 중 추가로 찾음: **불광동호박나이트(손흥민)** 판 번호 표에 `-v6` 이 있는데 그 파일이 어느 커밋에도 없어 09-07 부터 기본 그림(`nolcool-og.jpg`)이 나가고 있었다 → 표준 4줄 카드 `-v6` 을 만들어 넣음.
- 광고주 10명 카드 그림 속 글자 전부 기준과 일치(눈 대조) · 빌드 결과물 대조 80항목 실패 0 · 금지 이미지 해시 0.

# 2일째 판 — 준비 끝 (커밋 80cbad6)
- 들어간 것: 긴급 수원찬스돔 · 불광동호박 카드 · 홈 자동완성 잘림 · 로그인·환영 쪽 네이버 문구.
- 빌드 39 exit 0 · 게이트 466쪽 막음 0 · 11-4 디버깅 343/0 · 11-5 스테이징 135/0·디버깅 155/0 · 광고주 대조 80/0.
