# [놀쿨11-4] 10쪽 루프 · 홈 검색창 · 네이버형 홈 · 비교/퀴즈/랭킹 진입 — 결과 (2026-09-24 · 로컬 · 배포 0)

모델: Opus 5 (대표님이 세션 중 바꾸심 · 앞 단계 11-3 은 Fable 5.1) · 세션 4411ac5c · 코드는 worktree `D:\naver-watch\wt-nolcool`(브랜치 nol11-2 · origin/main 38be1ef 기준) — 작업본 `repos/Ilsanroom` 손대지 않음 · 푸시 0 · 주소 변경 0 · 네이버 0 · 새 데이터 엔진 0.

## 판정 한 줄
**2-1~2-5 를 전부 넣고 빌드 33회차가 게이트 41개 + page-gate 466쪽 막음 0 으로 완주했다.** 검증 스테이징 130 · 디버깅 343 · 최종 231, 실패 0. 다만 **브라우저 실측(모바일 화면·콘솔 오류·GA4 이벤트 찍힘)은 실행 불가**다 — 로컬 미리보기 서버가 시스템 메모리 부족으로 정지됐고, 다시 띄우는 것은 대표님 지시가 있을 때만 한다. 그래서 「자신있습니다」는 쓰지 않는다(아래 6절).

## 1. 겹침 — 있는 것 그대로 쓴 것 (새로 만든 데이터 0)
| 있던 것 | 그대로 쓴 방식 |
|---|---|
| 인기 엔진 `scripts/popularity-engine.mjs`(조회1·사용자2·전화30·검색3 · 28일 창 · 침묵 10회) | 순위 값의 **유일한 출처**. 프리렌더가 `POP_RANK`(순위 있는 57곳)로 읽어 다음에 볼 곳 차례를 정한다 |
| 랭킹 `/ranking/` · 퀴즈 `/quiz/` · 주말 `/weekend/` · VS `/vs/` | 목록·허브·커뮤니티 쪽의 「주말 추천 · 퀴즈로 고르기 · 랭킹 보기」 진입으로 이음 |
| 매거진 59편(`venueSlug`·본문 링크) | 가게 쪽 「관련 매거진」 1칸 — 점수 잣대는 화면 컴포넌트 `RelatedMagazineForVenue` 와 같은 값(100·100·90·60·20·10) |
| 커뮤니티 9판 · 지역 허브 40+ · 업종 6 | 허브 묶음·모듈 칸으로 이음 |
| `/compare/` 쪽 · `useCompareList`(최대 4) | 비교 진입을 **검색 매개변수**로만 붙임(새 주소 0) |
| GA4 태그 1개 · `visitor-tracker.ts` | 이벤트 4종을 기존 `send()` 게이트 뒤에 더함(새 태그·새 메일 0) |

## 2. 고친 것
### 2-1 모든 쪽 「다음에 볼 곳」(11-2 가 비워 둔 자리 `data-nc-next="slot"`)
- **가게 126쪽 = 8칸**: 같은 지역 다른 가게 3 + 같은 업종 다른 지역 2 + 지역 허브 1 + 관련 매거진 1 + 비교 진입 1.
  - 차례는 **인기 엔진 값 순**. 같은 지역에 다른 가게가 3곳 미만이면 같은 업종 다른 지역 → 전국 인기 순으로 채워 **126/126 전부 8칸**.
  - **같은 쪽에 같은 주소 2번 0** — 본문·정리·허브 묶음에 이미 있는 링크는 건너뛰고 다음 후보를 쓴다.
  - 매거진 칸 **126/126**, 광고 표시 규칙 그대로(전화 단추는 본문에 광고주 번호가 있을 때만).
- **목록·허브 229쪽**: 「이 지역 지금 인기 5」(그 쪽 가게 중 엔진 순위가 있는 곳) + 주말·퀴즈·랭킹. 가게가 1~3곳뿐인 허브는 같은 이름만 되풀이돼 스터핑에 걸리므로(실측 `/near/월평` 3.76%) **그 쪽에 없는 전국 인기 5**를 채운다 — 값은 같은 엔진, 이름은 서로 다르다. 인기 절이 붙은 쪽 **226/229**.
- **매거진 59쪽**: 본문에 실제로 링크된 가게 3 + 지역 허브 + 다음 글(같은 태그 최근순) + 랭킹·매거진 전체. 본문 가게 칸이 붙은 쪽 42/59(나머지는 본문에 가게 링크가 없는 글).
- **커뮤니티·안내**: 전국 인기 5 + 주말·퀴즈·랭킹.
- **새 창 0 · 자동 이동 0 · 무한 스크롤 속임 0 · 클릭 유도 팝업 0**(게이트 M3·L1 로 막음).

**쪽당 내부 링크(본문 안 · 중복 제거) 전 → 후**

| 유형 | 전(중앙) | 후(중앙) | 후(최소~최대) |
|---|---|---|---|
| 가게 126 | 16 | **22** | 21~28 |
| 업종·지역 목록 33 | 12 | **19** | 13~108 |
| 지역·역·태그 허브 196 | 9 | **15** | 11~36 |
| 매거진 59 | 10 | **13** | 10~17 |
| 커뮤니티 16 | 7 | **14** | 14~15 |
| 안내 35 | 7 | **13** | 9~60 |

466쪽 중 **457쪽이 12개 이상**(전 328쪽). 목표 12~25 밖은 34쪽(품질 경고) — 가게 9쪽이 26~28개(본문에 이미 주변 6곳·태그가 있어서), 나머지는 가게 1~2곳짜리 작은 허브 11개다.

### 2-2 홈 검색창(네이버형 홈)
- **프리렌더 홈**: 첫 화면 맨 위 `<form action="/search/">` + `<datalist>` 자동완성 **190항목**(가게이름 126 + 지역 + 업종 6 + 지역×업종) → JS 없이도 검색된다. 이어서 업종 탭 6 · 지역 바로가기 12(가게 수 순) · 신규 입점 · 주간 투표.
- **React 홈**: `src/components/home/TopSearchBar.tsx` — 가게이름·지역·업종·지역×업종 자동완성 8개, 로컬 장부만 읽고 **외부 호출 0**, 제출하면 `/search?q=`(있는 검색 쪽). 장부(462KB)는 **첫 입력 때만 동적으로** 읽어 홈 첫 화면 번들에 싣지 않는다.
- 홈 JSON-LD `WebSite`+`SearchAction`(11-3)의 `urlTemplate` 과 같은 주소 꼴이다.
- 오늘 인기·신규·주간 투표·커뮤니티 최신·매거진 최신은 **이미 홈에 있던 절**을 그대로 둔다(겹쳐 만들지 않음).

### 2-3 비교 도구
- 기존 `/compare/` 를 그대로 쓰고 **검색 매개변수**로 받는다: `/compare/?v=<자기>,<같이 볼 곳>`(장부 슬러그만 · 최대 4 · 사이트맵 변화 0 · 새 주소 0).
- 가게 쪽 비교 칸이 그 쪽에서 고른 가게를 그대로 넘기므로 눌렀을 때 바로 2~3곳이 나란히 놓인다.
- 값이 없는 항목은 줄을 그리지 않는 기존 동작 그대로(빈칸 0).

### 2-4 완독 → 다음 행동
- 쪽 끝(한 줄 정리 아래) = **다음에 볼 곳 + 저장 단추 + 전화**(있는 규칙대로 광고주 번호가 있을 때만).
- 저장은 회원 기능(11-5)과 같은 자리 — 비회원은 로컬 저장(`nolcool_favorites`, 화면 `useFavorites` 와 같은 열쇠·같은 슬러그)이라 로그인하면 그대로 이어진다. 가짜 숫자(「N명이 저장」) 0.

### 2-5 측정(있는 GA4 태그에 더함 · 새 태그 0)
`scroll_90`(세로 90% · 쪽당 1회) · `next_click`(어느 모듈을 눌렀나) · `compare_open`(2곳 이상 나란히) · `search_use`(홈 검색창). 넷 다 기존 `send()` 게이트(봇·관리자·내부 제외) 뒤에서만 나간다. `pages_per_session` 은 GA4 기본값이라 따로 만들지 않았고, 대시보드는 월간 지휘자 보고에 붙인다(새 메일 0).

### 게이트 확장 (`scripts/page-gate.mjs`)
막음 4가지 추가 — **M1** 다음에 볼 곳 안 같은 주소 2번 · **M2** 가게 쪽 7칸 미만 · **M3** 모듈 링크 새 창 · **M4** 가게 쪽 저장 단추 없음. 품질 1가지 — **Q4** 본문 내부 링크 12~25 밖.

## 3. T1~T6
| # | 무엇 | 결과 |
|---|---|---|
| T1 | 가게 표본 20: 8칸 · 중복 0 · 진짜 값 순 · 새 창 0 | **20/20 통과**(126쪽 전부 8칸) |
| T2 | 허브·목록 표본 10: 인기 5 = 인기 엔진 값 | **10/10 통과** |
| T3 | 검색창: 자동완성 · 결과 쪽 · 외부 호출 0 | **정적·번들 검사 통과** · 화면 실측은 실행 불가(6절) |
| T4 | 비교: 사실만 · 사이트맵 변화 0 | 통과 |
| T5 | GA4 이벤트 | **코드·번들에 4종 확인** · 디버그 뷰 실측은 실행 불가(6절) |
| T6 | 빌드 · 주소 0 · 게이트 exit 0 · 번들 전/후 | 통과 — 빌드 33회차 exit 0 · 사이트맵 466=466(빠짐 0·추가 0) · 번들 2,889,100 → **2,893,554 bytes(+4,454 · +0.15%)** |

## 4. 검증 3단계 (`scripts/verify/nc11-4-check.mjs`)
| 단계 | 검사 | 통과 | 실패 | 실행 불가 |
|---|---|---|---|---|
| 스테이징 | 130 | 130 | 0 | 0 |
| 디버깅 | 343 | 343 | 0 | 0 |
| 최종 | 231 | 230 | 0 | 1(브라우저) |

기록 파일 `docs/nc11-4-verify-*.json`.

## 5. 대표님만 하실 수 있는 것
1. **로컬 미리보기 서버 다시 띄우기**(브라우저 실측을 마치려면 필요). 터미널에 이 한 줄:
   ```
   ! cd /d/Temp/claude/C--Users-admin-naver-watch/4411ac5c-957b-4b6a-a0e5-be85afc1c77c/scratchpad/nol11 && node serve-dist.mjs D:/naver-watch/wt-nolcool/dist 4173
   ```
   띄워 주시면 모바일 412px 화면·콘솔 오류·GA4 이벤트 찍힘을 재고 최종 단계에 붙입니다.
2. GA4 디버그 뷰는 구글 계정 권한이 있어야 열립니다(11-1 실측 「GA4 권한 없음」).

## 6. 못 찾은 것 · 못 한 것
- **브라우저 실측(T3 화면·T5 디버그 뷰·모바일·콘솔 오류)** — 로컬 서버가 시스템 메모리 부족으로 정지됐고, 되살리는 것은 대표님 몫으로 뒀습니다. 그래서 「자신있습니다, 작업 완벽합니다」를 쓰지 않습니다.
- **인터넷 100회 대조** — 새로 하지 않았습니다. 11-4 가 기대는 외부 규칙(구글 링크 권장사항 G-links · 스팸 정책 G-spam · GA4 향상측정 90% · GA4 권장 이벤트)은 11-1 연구 145건에 200 응답으로 들어 있어 그것을 근거로 썼습니다.
- **쪽당 내부 링크 숫자 기준** — 지시서는 「연구 4 로 확정」이라 했지만 **11-1 연구 4절(성장·측정 24건)에 링크 수 기준이 없습니다(못 찾음)**. 그래서 지시서의 예시 12~25 를 우리 목표로 삼고, 가게 쪽은 **하한 12 를 규칙**으로, 상한 초과는 품질 경고로만 다뤘습니다.
- **외부 새 창 링크 3쪽** — `/cafe`(허용된 카페 1개) · `/rooms/ilsan/ilsanroom` · `/yojeong/ilsan/ilsanmyeongwolgwanyojeong`(광고주 공식 사이트). 11-4 이전부터 있던 것이라 손대지 않았습니다. 플랫폼 규칙의 「외부 링크 0」과 어긋나 보이므로 대표님 판단을 기다립니다.

## 7. 빌드·파일
빌드 26~33(8회 · 막힌 이유: 라운지 밀도 3.02% → 인기 칸 라벨에서 업종어 뺌 · 작은 허브 스터핑 3.76% → 전국 인기로 대체 · 같은 지역 3곳 미만 106쪽 → 같은 업종·전국 인기로 채움 · 가게이름 밀도 3.01% → 매거진·비교 라벨에서 이름 뺌 · 변형 엔진이 `<li>`→`<div>` 로 바꿔 모듈 칸이 1로 잡힘 → `data-nc-module` 로 직접 셈).
고친 파일: `scripts/prerender-seo.mjs` · `scripts/skeleton/index.mjs` · `scripts/page-gate.mjs` · `src/lib/visitor-tracker.ts` · `src/components/seo/SsrArticle.tsx` · `src/pages/ComparePage.tsx` · `src/pages/HomePage.tsx` · 새 파일 `src/components/home/TopSearchBar.tsx` · `scripts/verify/nc11-4-check.mjs` · `docs/nc11-4-verify-*.json`.

다음 단계: 놀쿨11-5
