.venue-deep.mjs
.click-tmp.mjs
.season*.mjs
.gh-secret-set.mjs
# IndexNow 제출 스냅샷 — actions/cache로 영속(repo 커밋 X). .seo-lastmod.json은 커밋 대상이라 제외 안 함
scripts/.indexnow-last.json
# lastmod-honesty-gate용 직전 매니페스트 덤프 — 빌드 한정 산출물(repo 커밋 X)
scripts/.seo-lastmod.prev.json
# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/versions

# testing
/coverage

# next.js
/.next/
/out/

# production
/build
/dist

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.pnpm-debug.log*

# env files (can opt-in for committing if needed)
.env*

# vercel
.vercel

# OpenNext (Cloudflare Workers build output)
.open-next/

# typescript
*.tsbuildinfo
next-env.d.ts
.gstack/
.claude/skills/gstack/
supabase/.temp/
nolcool-dist/
