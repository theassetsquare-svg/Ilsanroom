/**
 * 이 프로젝트는 PostCSS 플러그인을 쓰지 않는다.
 * Tailwind 는 vite 플러그인(@tailwindcss/vite)으로 직접 붙는다 — vite.config 참고.
 *
 * ★ 2026-08-25 — 그런데 왜 빈 설정을 굳이 두는가
 *   PostCSS 는 설정 파일이 없으면 **상위 폴더를 계속 거슬러 올라가며** 찾는다.
 *   저장소가 놓인 위치에 따라 남의 프로젝트 설정을 물어오고, 그러면
 *   설치도 안 된 플러그인을 부르며 빌드가 통째로 실패한다(실제로 겪음).
 *   빌드 결과가 "저장소가 어느 폴더에 있느냐"에 좌우되면 안 된다.
 *   여기서 탐색을 끊어, 어디에 두든 똑같이 빌드되게 한다.
 */
export default { plugins: {} };
