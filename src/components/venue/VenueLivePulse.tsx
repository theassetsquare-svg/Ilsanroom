// Fabricated live viewer/favorite messages removed — no unsourced data per trust policy.
// Component kept as null to preserve existing import sites.
export default function VenueLivePulse(_: {
  slug: string;
  isPremium?: boolean;
  initialViewing: number;
}) {
  return null;
}
