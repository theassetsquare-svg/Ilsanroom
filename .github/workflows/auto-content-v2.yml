name: Auto Content v2 (DISABLED — 가짜 UGC 금지)

# 2026-08-09 파트2.5 정직화: 가짜 회원 글 자동 생성 영구 중단.
# 근거: 사이트 #1 규칙 "가짜 0" + 표시광고법 리스크. 커뮤니티는 100% 진짜 회원 글만.
# schedule 제거됨. 스크립트(auto-content-v2.mjs)도 실행 시 즉시 종료 가드 내장.
# 이 워크플로/스크립트를 되살리려면 community-no-fake-seed-gate 통과 불가(빌드 차단).
on:
  workflow_dispatch: {}

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Refuse — 가짜 콘텐츠 자동 생성 영구 금지
        run: |
          echo "❌ auto-content-v2는 2026-08-09 정직화로 영구 비활성화되었습니다."
          echo "커뮤니티는 진짜 회원 글만 노출합니다 (가짜 0)."
          exit 1
